let firstName : string = "Jonathan";
let lastName  : string = "Alisonsky";

console.log(`The strings length' are ${firstName.length} and ${lastName.length} characters wide.`);

let fullName :string = firstName + ' ' + lastName;
console.log(fullName);
console.log(`The full name ist ${fullName.length} characters wide.`);
